Usage:

```bash
cd src # or wherever you'd like
mkdir daybreak
cd daybreak
degit Seiyial/PACK/daybreak-ts/core # DkResult and error codes scaffold
degit Seiyial/PACK/daybreak-ts/hooks # react hooks library; uses immer
# ...
```